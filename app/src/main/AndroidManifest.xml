<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adhd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ADHD">
        <activity android:name=".fonctionnement_demarche"></activity>
        <activity android:name=".sous_menu_en_parler" />
        <activity android:name=".sous_menu_demarche" />
        <activity android:name=".MieuxVivreSonTDAH" />
        <activity android:name=".explications" />
        <activity android:name=".bienvenue" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".activity_inscription" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>